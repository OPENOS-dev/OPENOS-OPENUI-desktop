# OPENUI — OPENOS 统一界面设计规范

版本：DEV2026.1 · 状态：草案 v1 · 适用：OPENOS 桌面环境（Wayland/wlroots）

> OPENUI 是 OPENOS 的**设计语言**。它以 NUI2（NOTHING UI 2）的视觉基调
> （深色 / 20 层叠层 / 动态模糊 / 文字符号 / 键盘优先）为底色，参考
> **Material Design 3** 在**色彩系统、状态层叠、文字排印、形状、动效、
> 间距网格、对比度、触控目标**等维度上的成熟做法，凝练出 OPENOS 自己的
> 统一界面令牌。本文档与 `src/openui.h`（C 令牌实现）一一对应。

---

## 1. 设计原则

1. **极简深色**：永远从最深的 `#090909` 出发，用层级而非边框表达结构。
2. **内容即界面**：文字符号（禁 Emoji）承载图标语义，键盘优先于鼠标。
3. **毛玻璃分级**：浮层（面板/菜单/通知）下的内容做动态模糊（合成器实现），
   浮层自身半透明（透明度偏高，让模糊更明显），形成"玻璃堆叠"的深度感。
4. **柔和圆角**：圆角比 NUI2 原始基线更大（基础 8px、卡片 12px、浮窗 24px），
   更现代柔和、贴合普通用户习惯；避免过度，仍以功能性为主。
5. **动效为意义服务**：一切动画都有语义（状态变化/层级变化/进入退出），
   时长取自统一阶梯，缓动取自统一曲线。
6. **亲和易用**：清晰 hover/按下反馈、柔和的激活态、底部分隔阴影，
   让不熟悉 Linux 的用户也能直觉操作。

---

## 2. 色彩系统

### 2.1 中性色板（表面层）

表面用 L\* 步长 4 的 20 级灰阶表达"叠层深度"。层越高越亮。

| 令牌 | 值 | 用途 |
|---|---|---|
| `OUI_BACKGROUND` / surface-0 | `#090909` | 桌面根背景 |
| `OUI_SURFACE` / surface-1 | `#141414` | 默认表面（窗口/面板底） |
| surface-2 … surface-6 | `#1C1C1C` … `#3F3F3F` | 抬高层级（菜单/通知/浮窗） |
| surface-19 | `#C2C2C2` | 最高抬高层级 |

### 2.2 语义色（MD3 派生）

| 角色 | 主色 | on 色 | 容器色 | on 容器色 |
|---|---|---|---|---|
| **Primary** | `#00BCD4` | `#001014` | `#006A7A` | `#C9F5FF` |
| **Secondary** | `#6EB3C0` | `#001317` | `#3C6B75` | `#D8F2F7` |
| **Tertiary** | `#9FC85F` | `#111A00` | — | — |
| **Error** | `#F44336` | `#FFFFFF` | `#8C1D18` | `#FFDAD6` |

- 主色用于：焦点状态、激活指示、强调按钮、任务栏激活项、工作区激活指示。
- 容器色用于：选中容器、状态胶囊、高亮背景。
- 错误色仅用于：错误通知、破坏性操作、输入校验失败。

### 2.3 状态层叠（MD3 state layers）

交互状态通过"在容器色上叠加半透明 on-surface"表达，不改变容器色本身：

| 状态 | 叠加 alpha |
|---|---|
| Hover（悬停） | 8% |
| Focus（键盘焦点） | 12% |
| Pressed（按下） | 12% |
| Dragged（拖拽） | 16% |

C 端用 `oui_state_overlay(hex, alpha)` 计算。

---

## 3. 文字排印（Type Scale）

字体族默认 `Sans`（后续替换为 OPENOS 自带字体），等宽 `Monospace`。

| 样式 | 字号/行高 | 字重 | 用途 |
|---|---|---|---|
| Display L/M/S | 57/45/36 ÷ 64/52/44 | 400 | 锁屏/开机欢迎（极少用） |
| Headline L/M/S | 32/28/24 ÷ 40/36/32 | 400 | 设置页标题 |
| Title L | 22/28 | 400 | 对话框标题 |
| Title M | 16/24 | 500 | 列表组标题、任务栏激活项 |
| Title S | 14/20 | 500 | 面板小节标题 |
| Body L/M/S | 16/14/12 ÷ 24/20/16 | 400 | 正文/通知正文 |
| Label L/M/S | 14/12/11 ÷ 20/16/16 | 500 | 按钮、标签、胶囊 |

### 3.1 对比度层级

文本在表面上的对比度取 87% / 60% / 38% 三档（MD3 的 on-surface 变体）：
- 87%：主文本、标题
- 60%：次级文本、辅助说明
- 38%：禁用、占位符

C 端用 `oui_text(hex, level)` 计算。

---

## 4. 形状（圆角）

圆角比 NUI2 原始基线更大，更柔和现代（普通用户习惯）：

| 令牌 | 值 | 用途 |
|---|---|---|
| `OUI_SHAPE_NONE` | 0 | 图像、全屏浮层 |
| `OUI_SHAPE_XS` | 8px | 按钮、输入框、任务栏条目、胶囊 |
| `OUI_SHAPE_SM` | 12px | 卡片、菜单、通知卡片 |
| `OUI_SHAPE_MD` | 16px | 对话框 |
| `OUI_SHAPE_LG` | 24px | 大浮窗、应用抽屉、命令面板 |
| `OUI_SHAPE_FULL` | 9999 | 工作区指示器、开关、进度点 |

毛玻璃透明度（`OUI_GLASS_*_ALPHA`）：面板 0.72 / 卡片 0.75 / 菜单 0.78，
透明度偏高以让合成器动态模糊更明显（模糊而非直接透明）。

---

## 5. 动效（Motion）

### 5.1 时长阶梯

| 令牌 | 值 | 典型用途 |
|---|---|---|
| `OUI_DUR_50` | 50ms | 按下即时反馈、光标变化 |
| `OUI_DUR_100` | 100ms | 小型状态切换、指示器移动 |
| `OUI_DUR_150` | 150ms | 按钮/条目 hover 态 |
| `OUI_DUR_200` | 200ms | 浮层淡入淡出、任务栏新增 |
| `OUI_DUR_250` | 250ms | 菜单/通知进入退出 |
| `OUI_DUR_300` | 300ms | 面板展开/收起、工作区切换 |
| `OUI_DUR_400` | 400ms | 对话框、较大结构动画 |
| `OUI_DUR_500` | 500ms | 欢迎/锁屏过渡 |

### 5.2 缓动曲线

| 曲线 | 贝塞尔 | 语义 |
|---|---|---|
| `standard` | `cubic-bezier(0.2, 0.0, 0.0, 1.0)` | 常规进入/离开 |
| `decelerated` | `cubic-bezier(0.0, 0.0, 0.0, 1.0)` | 进入场景（从快到慢） |
| `accelerating` | `cubic-bezier(0.3, 0.0, 1.0, 1.0)` | 离开场景（从慢到快） |
| `emphasized` | `cubic-bezier(0.2, 0.0, 0.0, 1.0)` | 重要强调动画 |

规则：**进入用 decelerated，离开用 accelerating，状态强调用 emphasized**。

---

## 6. 间距网格与布局

- 基础网格 4dp，常用间距：4 / 8 / 12 / 16 / 20 / 24 / 32（`OUI_SP_1..8`）。
- 组件内边距：按钮 12×8；卡片 16；列表项 16×12。
- 面板高度 `OUI_PANEL_HEIGHT = 32`。
- 触控目标：最小 48×48dp；指针目标：最小 24×24dp。

### 6.1 面板布局（自左向右）

```
[ 品牌标识 ] [ 工作区指示器 ▪▪▪▪ ] [ 任务栏窗口 ……………………… ] [ 时钟 ] [ ☰ Menu ]
```

- 工作区指示器：4 个 16×4 胶囊，激活项主色、其余 surface-7；
  点击胶囊切换工作区。
- 任务栏：当前工作区的窗口按钮（Title S，激活项主色文字 + surface-6 底）；
  左键激活、右键关闭、多窗口超出后滚动/截断。

---

## 7. 组件语义

| 组件 | 表面 | 圆角 | 状态表达 |
|---|---|---|---|
| 按钮（主） | primary | XS(4) | hover/按下叠加状态层 |
| 按钮（文本） | 无底 | XS | 文字主色，hover 状态层 |
| 卡片 | surface-2 | SM(8) | 选中加 primary 描边 1px |
| 菜单/启动器 | surface-6（玻璃） | SM(8) | 条目 hover 状态层，键盘↑↓导航 |
| 通知卡片 | surface-6（玻璃）+ primary 竖条 | SM(8) | 自动过期，点击消除 |
| 对话框 | surface-5 | MD(12) | 遮罩 60% 黑 + 无模糊 |
| 工作区胶囊 | surface-7 | FULL | 激活 primary |

---

## 8. 无障碍

- 所有可交互目标 ≥ 24px（指针），关键操作 ≥ 48px（触控）。
- 文本对比度遵守 87/60/38 层级，主文本对 surface-1 ≥ 4.5:1。
- 全界面可仅用键盘完成：Tab 焦点环（focus 状态层 12%）、↑↓←→ 导航、
  Ctrl+K 命令面板、Ctrl+1..4 切工作区、Esc 关闭。
- 不使用颜色作为唯一信息载体（配合文字符号与形状）。

---

## 9. 图标（文字符号，禁 Emoji）

沿用 NUI2：`☰ ⌕ ⚙ × ⌂ ⚛ → ✓ ✕`。新图标需求一律用 Unicode 文本符号或
自绘矢量，禁止 Emoji 表情。

---

## 10. 落地对照

| 文件 | 内容 |
|---|---|
| `src/openui.h` | 全部令牌的 C 常量与内联工具（`oui_token`/`oui_state_overlay`/`oui_text`） |
| `src/nui2.h` | 视觉基线（20 层表面色/强调色/文字符号），被 openui.h 覆盖并扩展 |
| `src/compositor.c` | 合成器侧：模糊、工作区、窗口管理 |
| `src/panel.c` | 面板/任务栏/工作区切换器客户端 |
| `src/notifyd.c` | 通知层客户端 |

后续：动效时长与缓动由合成器动画调度器落地；字体替换为 OPENOS 自研字体；
高对比度主题与跟随系统强调色作为远期项。
